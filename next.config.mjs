/** @type {import('next').NextConfig} */
const nextConfig = {
  //domains to fetch image from
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
      {
        protocol: "http",
        hostname: "**",
      },
    ],
  },
};

export default nextConfig;
